spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/doitpay
    username: doitpay
    password: doitpay
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  rabbitmq:
    host: localhost
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 1